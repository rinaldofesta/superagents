# Docker Skill

> Container platform for building, shipping, and running applications

## When to Use

Use this skill when containerizing applications, writing Dockerfiles, or using Docker Compose.

## Key Concepts

| Concept | Description | Use Case |
|---------|-------------|----------|
| Image | Immutable template | Application packaging |
| Container | Running instance of image | Isolated execution |
| Dockerfile | Build instructions | Create custom images |
| Compose | Multi-container orchestration | Development environments |
| Volume | Persistent data storage | Database data |

## Dockerfile Best Practices

```dockerfile
# Use specific version, not 'latest'
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files first (better caching)
COPY package*.json ./

# Install dependencies
RUN npm ci --only=production

# Copy source code
COPY . .

# Build application
RUN npm run build

# Use non-root user
USER node

# Expose port
EXPOSE 3000

# Health check
HEALTHCHECK --interval=30s --timeout=3s \
  CMD curl -f http://localhost:3000/health || exit 1

# Start command
CMD ["node", "dist/index.js"]
```

## Multi-stage Build

```dockerfile
# Build stage
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Production stage
FROM node:20-alpine
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
COPY package*.json ./
USER node
EXPOSE 3000
CMD ["node", "dist/index.js"]
```

## Docker Compose

```yaml
# docker-compose.yml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/myapp
    depends_on:
      - db
    volumes:
      - ./src:/app/src  # Development hot-reload

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: myapp
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
```

## Common Commands

```bash
# Build image
docker build -t myapp:latest .

# Run container
docker run -d -p 3000:3000 --name myapp myapp:latest

# Docker Compose
docker compose up -d        # Start all services
docker compose down         # Stop all services
docker compose logs -f app  # Follow logs
docker compose exec app sh  # Shell into container

# Cleanup
docker system prune -a      # Remove unused data
docker volume prune         # Remove unused volumes
```

## .dockerignore

```
node_modules
npm-debug.log
dist
.git
.env
*.md
.DS_Store
```

## Pitfalls

- Don't run as root in production
- Use specific image tags, not `latest`
- Don't store secrets in images
- Use .dockerignore to reduce image size
- Order Dockerfile instructions for cache efficiency

## Context7

```
mcp__context7__resolve-library-id with "docker"
mcp__context7__query-docs for specific Docker questions
```

---

Generated by SuperAgents
